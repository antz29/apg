{
  "layer": "requirements",
  "type": "requirement",
  "name": "freshness-content-key",
  "body": "Freshness must be judged by content identity — the recorded git state plus working-tree/index/untracked content — never by file mtime. AC: a dirty tree at the same HEAD sha is not fresh and does not take the fast-path; a clean tree at the same sha with the same content does.",
  "properties": {
    "feature": "scan-freshness"
  },
  "out": [
    {
      "kind": "drives",
      "target": "domain.entity.content-manifest",
      "properties": {}
    }
  ],
  "in": [
    {
      "kind": "contains",
      "source": "requirements.requirement.freshness-fast-path",
      "properties": {}
    }
  ]
}