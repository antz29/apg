{
  "layer": "requirements",
  "type": "requirement",
  "name": "cross-worktree-cache-sharing",
  "body": "The manifest and per-file fact cache must be shared across worktrees and branches of the same repository, because a fresh project worktree is near-identical to main. AC: `apg project start` on a clean, near-identical worktree reuses cached facts and completes far faster than a cold full scan, without weakening exactness.",
  "properties": {
    "feature": "incremental-content"
  },
  "out": [],
  "in": [
    {
      "kind": "contains",
      "source": "requirements.requirement.incremental-content",
      "properties": {}
    }
  ]
}