{
  "layer": "requirements",
  "type": "requirement",
  "name": "language-rooting",
  "body": "Feature language-rooting (user decision — Human decision, folded into the scanner-optimisation change-set; do not re-litigate). A `Language` code-graph node kind plus language-rooted FQNs: every scanned language becomes the disjoint root of its own module namespace, so cross-language module/symbol FQN collisions are impossible by construction.\n\nThe ingestor derives one `Language` node per language actually scanned from the existing `lang_switch` control record (NOT scanner-emitted), makes each module a child of its language node (`Language —Contains→ Module`), and prefixes every module FQN with the language id (symbols inherit through `parent.name`). Today module FQNs are emitted verbatim with no cross-language namespace and the ingestor SILENTLY SHADOWS a module whose FQN is already claimed (module-vs-module or module-vs-type) — silent data loss; only same-kind Struct/Function collisions panic.\n\nBecause every code FQN changes, every existing authored `implemented-by` target drifts until migrated to its rooted FQN (sequenced: the rooted-FQN change lands in code → every authored target's rooted destination is declared as a planned Implementation node so it grades Pending, never Drift → every `implemented-by` edge is re-pointed bare→rooted → the branch re-scan realizes the planned nodes and confirms no drift; see requirements.constraint.implemented-by-migration-sequence).\n\nDecomposes into the Language node kind, the Language→Module containment, the rooted module FQN, symbol inheritance, cross-language collision-freedom, the implemented-by migration, and the FQN-convention docs/tooling update. Feature language-rooting.",
  "properties": {
    "feature": "language-rooting"
  },
  "out": [
    {
      "kind": "contains",
      "target": "requirements.requirement.language-node-kind",
      "properties": {}
    },
    {
      "kind": "contains",
      "target": "requirements.requirement.language-contains-module",
      "properties": {}
    },
    {
      "kind": "contains",
      "target": "requirements.requirement.language-rooted-module-fqn",
      "properties": {}
    },
    {
      "kind": "contains",
      "target": "requirements.requirement.symbols-inherit-language-root",
      "properties": {}
    },
    {
      "kind": "contains",
      "target": "requirements.requirement.cross-language-collision-freedom",
      "properties": {}
    },
    {
      "kind": "contains",
      "target": "requirements.requirement.implemented-by-migration",
      "properties": {}
    },
    {
      "kind": "contains",
      "target": "requirements.requirement.language-rooting-docs",
      "properties": {}
    },
    {
      "kind": "drives",
      "target": "domain.group.frontend-coverage",
      "properties": {}
    },
    {
      "kind": "drives",
      "target": "domain.value.language-id",
      "properties": {}
    }
  ],
  "in": [
    {
      "kind": "contains",
      "source": "requirements.stakeholder.polyglot-repo-team",
      "properties": {}
    },
    {
      "kind": "contains",
      "source": "requirements.stakeholder.apg-maintainer",
      "properties": {}
    },
    {
      "kind": "contains",
      "source": "requirements.user.apg-cli-runner",
      "properties": {}
    },
    {
      "kind": "contains",
      "source": "requirements.user.suite-agent",
      "properties": {}
    },
    {
      "kind": "details",
      "source": "requirements.note.language-rooting-evidence",
      "properties": {}
    },
    {
      "kind": "details",
      "source": "requirements.note.language-rooting-open-points",
      "properties": {}
    }
  ]
}