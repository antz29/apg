{
  "layer": "solution",
  "type": "note",
  "name": "coordinator-feedback-routing",
  "body": "Design decisions for the coordinator-mediated feedback cycle [with user]:\n\n1. The reviewer keeps the terminal resolve/reject. Only the action step moves: the closed cycle is reviewer attaches (`open`) → the coordinator dispatches one writer per item and actions the item (`actioned`) → reviewer resolves (terminal) or rejects (reopen). Reviewers never action; writers never action.\n2. The writers are read-only on feedback: spec-writer, plan-writer, and implementer drop `apg_review_action`; they hold a read-only feedback channel (`apg_review`) and return an ACTIONED/WONT-FIX claim to the coordinator. (Judgment call: \"read-only\" is read as granting the read channel `apg_review`; flagged to the human.)\n3. One item per dispatch: the coordinator never batches feedback into a single writer dispatch.\n4. The coordinator's review of the claim is shallow consistency, not correctness: ACTIONED with no change / WONT-FIX with changes is a mismatch → re-dispatch; a consistent claim → action it.\n5. The coordinator gains `apg_review_action` (it had `apg_review` only); the reviewers are unchanged (add/resolve/reject).\n\nRelationship to `requirements.requirement.review-surface-unchanged`: this change does NOT alter the `apg review` subcommand surface (`add|action|resolve|reject|list`) — only the agent grants and the prompt/tool prose that drive it. The two are consistent, not conflicting.\n\nAnchoring: agent prompts (`opencode-suite/agents/*.md`) and suite tool text (`opencode-suite/tools/*.ts`) are not scanned code; the `implemented-by` edges point at the install/embed Rust (`apg.install_suite`, `apg.main`) and the review CLI action path (`apg.review_cmd.review_action`), never at a `.md`/`.ts` path (see `solution.note.markdown-anchoring` / `solution.note.embedded-template-anchor`).\n\nCoverage caution for the plan-writer: the tier-4 delta is prose/template text embedded by a top-level `include_str!` constant that has no graph node; a plan task can only target a resolvable code FQN, so this component's anchors are delivery-path anchors. If the spine-scoped coverage gate demands a branch-added solution node's FQNs be touched, the plan should claim the install/action anchors it genuinely realizes rather than invent an edit to unchanged code.",
  "properties": {},
  "out": [
    {
      "kind": "details",
      "target": "solution.component.coordinator-feedback-routing",
      "properties": {}
    }
  ],
  "in": []
}