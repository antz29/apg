{
  "layer": "solution",
  "type": "component",
  "name": "write-through-projection",
  "body": "The write-through projection — SUPERSEDES the write-back/batched buffer model (`solution.component.derived-projection-buffer`, removed). Every completed mutation applies its exact node/edge delta to `apg/.trans/db.lbug` synchronously as part of the mutation: the durable file write + git commit first (commit-then-project), then the delta — with no accumulation, no deferred flush, and no session-end dependency.\n\nWhen a session is live the coordinator applies each routed mutation's delta through this component before acknowledging; the direct path applies it inline. Amortization keeps one DB open/parse across the session but never defers visibility: `coordinator-amortized-flush` is amortization of the open, not batching of visibility.\n\nIt anchors to the real synchronous write paths — `apg.layers.write_project` (durable node/edge path) and `apg.plan_cmd.write_through` (transient plan/feedback path) — and to `solution.component.projection-delta-applier` for the exact-delta deletion semantics. The session-side synchronous apply inside `apg.session.Coordinator` is now realized (see `solution.container.mutation-coordinator`).",
  "properties": {},
  "out": [
    {
      "kind": "implemented-by",
      "target": "rust.apg.layers.write_project",
      "properties": {}
    }
  ],
  "in": [
    {
      "kind": "realised-by",
      "source": "domain.entity.derived-projection",
      "properties": {}
    },
    {
      "kind": "realised-by",
      "source": "domain.entity.projection-delta",
      "properties": {}
    },
    {
      "kind": "realised-by",
      "source": "domain.service.projection-update",
      "properties": {}
    },
    {
      "kind": "contains",
      "source": "solution.container.mutation-coordinator",
      "properties": {}
    },
    {
      "kind": "details",
      "source": "requirements.note.immediate-queryability-reconciliation",
      "properties": {}
    }
  ]
}