{
  "layer": "solution",
  "type": "component",
  "name": "project-start-seeder",
  "body": "The `apg project start` branch-DB seeder: after the standard refusals it requires main's scan to be fresh for main's HEAD (refusing with `apg scan` guidance otherwise) and copies main's `apg/.trans` into the new worktree — no frontend scan.",
  "properties": {},
  "out": [
    {
      "kind": "implemented-by",
      "target": "rust.apg.project_cmd.project_start_at",
      "properties": {}
    },
    {
      "kind": "implemented-by",
      "target": "rust.apg.project_cmd.project_start",
      "properties": {}
    }
  ],
  "in": [
    {
      "kind": "contains",
      "source": "solution.container.scanner-cli",
      "properties": {}
    },
    {
      "kind": "realised-by",
      "source": "domain.entity.project-start-seed",
      "properties": {}
    }
  ]
}