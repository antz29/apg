{
  "layer": "solution",
  "type": "component",
  "name": "rust-manifest-discovery",
  "body": "Rust frontend manifest discovery: find all Cargo manifests under the scan root, load each project/workspace (rust-analyzer ProjectManifest/ProjectWorkspace), dedupe workspace members by manifest root, and merge the emitted facts. Fixes the discover_single gap that leaves src/rustlib (a separate Cargo project) out of the graph. Source is src/rustlib/src/main.rs (load_workspace_at / discover_single), which is not yet in the scanned graph — the plan declares it as a planned Implementation node.",
  "properties": {},
  "out": [],
  "in": [
    {
      "kind": "contains",
      "source": "solution.container.frontend-process",
      "properties": {}
    }
  ]
}