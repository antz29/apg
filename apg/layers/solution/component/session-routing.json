{
  "layer": "solution",
  "type": "component",
  "name": "session-routing",
  "body": "Transparent session routing in the `apg node` / `apg edge` dispatch: detect a live session socket under `apg/.trans/`, forward the mutation, and fall back to the existing direct path when no session is running. The CLI arguments, the `apg_node` / `apg_edge` tool surface, and every agent prompt stay unchanged. (Not yet built — planned code.)",
  "properties": {},
  "out": [],
  "in": []
}