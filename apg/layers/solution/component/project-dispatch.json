{
  "layer": "solution",
  "type": "component",
  "name": "project-dispatch",
  "body": "Subcommand routing inside `cmd_project`: start|merge|delete (unknown subcommands are refused with usage). Hosted by the existing `cmd_project` dispatch function — the `delete` arm is the change.",
  "properties": {},
  "out": [],
  "in": [
    {
      "kind": "contains",
      "source": "solution.container.project-command",
      "properties": {}
    }
  ]
}