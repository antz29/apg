{
  "layer": "solution",
  "type": "component",
  "name": "graph-loader",
  "body": "The existing full load path (src/load.rs: build_load_files, create_schema, copy_from, write_graph_jsonl). Retained as the fallback full-scan loader and the correctness reference the spliced DB must match.",
  "properties": {},
  "out": [
    {
      "kind": "implemented-by",
      "target": "apg.load.build_load_files",
      "properties": {}
    },
    {
      "kind": "implemented-by",
      "target": "apg.load.copy_from",
      "properties": {}
    }
  ],
  "in": [
    {
      "kind": "contains",
      "source": "solution.container.ingestor",
      "properties": {}
    }
  ]
}