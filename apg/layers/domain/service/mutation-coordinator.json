{
  "layer": "domain",
  "type": "service",
  "name": "mutation-coordinator",
  "body": "The domain service that makes concurrent mutation attempts safe and cheap: it serializes DB access under one writer, amortizes one DB open/parse across the session, and applies each mutation's exact delta write-through (never batched, never deferred to session end); it routes mutations transparently (auto-detect → forward → fall back to the direct path). It is the coordinator's behaviour, independent of process mechanics.",
  "properties": {},
  "out": [
    {
      "kind": "realised-by",
      "target": "solution.container.mutation-coordinator",
      "properties": {}
    },
    {
      "kind": "realised-by",
      "target": "solution.component.session-lifecycle",
      "properties": {}
    },
    {
      "kind": "realised-by",
      "target": "solution.component.session-routing",
      "properties": {}
    }
  ],
  "in": [
    {
      "kind": "contains",
      "source": "domain.group.mutation-surface",
      "properties": {}
    },
    {
      "kind": "drives",
      "source": "requirements.requirement.session-mutation-coordinator",
      "properties": {}
    },
    {
      "kind": "drives",
      "source": "requirements.requirement.coordinator-amortized-flush",
      "properties": {}
    }
  ]
}