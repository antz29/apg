{
  "layer": "domain",
  "type": "entity",
  "name": "feedback-item",
  "body": "A single unit of review feedback (a Feedback record) attached to an artifact node: the item the coordinator dispatches to exactly one writer and actions exactly once. It carries a body and a status (`open`/`actioned`/`resolved`); it is never combined with another item into one writer dispatch. Its body is immutable across the cycle — only status and disposition change.",
  "properties": {
    "kind": "entity"
  },
  "out": [
    {
      "kind": "realised-by",
      "target": "solution.component.coordinator-feedback-routing",
      "properties": {}
    }
  ],
  "in": [
    {
      "kind": "drives",
      "source": "requirements.requirement.coordinator-single-item-dispatch",
      "properties": {}
    }
  ]
}