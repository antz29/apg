{
  "layer": "domain",
  "type": "entity",
  "name": "project",
  "body": "A project context: a git local branch `refs/heads/<name>` + a linked worktree at `<main>/apg/.worktrees/<name>` + the branch DB at `<worktree>/apg/.trans/db.lbug`. Created by `start`; ended by `merge` (into the default branch) or `delete` (discard).",
  "properties": {
    "kind": "entity"
  },
  "out": [
    {
      "kind": "realised-by",
      "target": "solution.container.project-command",
      "properties": {}
    }
  ],
  "in": [
    {
      "kind": "drives",
      "source": "requirements.requirement.delete-subcommand",
      "properties": {}
    },
    {
      "kind": "contains",
      "source": "domain.group.project-lifecycle",
      "properties": {}
    }
  ]
}