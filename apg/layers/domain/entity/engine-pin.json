{
  "layer": "domain",
  "type": "entity",
  "name": "engine-pin",
  "body": "The exact engine version a frontend is built against, which sets the newest language construct that frontend can parse. Pinning it makes the language-version ceiling knowable. For TypeScript, C#, Python, and Markdown the ceiling is the pin alone; for Go, Java, and Rust the ceiling is the user's installed toolchain (the type-checker is the toolchain), so the pin records that jointly.",
  "properties": {
    "kind": "entity"
  },
  "out": [],
  "in": []
}