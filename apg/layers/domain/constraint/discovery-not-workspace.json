{
  "layer": "domain",
  "type": "constraint",
  "name": "discovery-not-workspace",
  "body": "Discovering a nested Cargo project must not make it a member of the root workspace. src/rustlib's isolation — separate Cargo.lock, isolated target/, pinned rust-analyzer git dependencies, edition = \"2021\" — is deliberate and load-bearing (build.rs); the fix for the nested-project gap is discovery, never workspace membership.",
  "properties": {
    "attaches-to": "domain.entity.cargo-manifest"
  },
  "out": [],
  "in": []
}