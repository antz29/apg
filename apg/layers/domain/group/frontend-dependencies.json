{
  "layer": "domain",
  "type": "group",
  "name": "frontend-dependencies",
  "body": "The frontend capability contract: every language apg ships a scanner for is a distinct frontend artifact with its own build-time dependencies, its own scan-time dependencies, and an engine pin that fixes the language-version ceiling. This group gathers those concepts so the question \"to scan language X, what do I need?\" has one answer.",
  "properties": {
    "attribute": "core"
  },
  "out": [
    {
      "kind": "contains",
      "target": "domain.entity.frontend-artifact",
      "properties": {}
    }
  ],
  "in": []
}